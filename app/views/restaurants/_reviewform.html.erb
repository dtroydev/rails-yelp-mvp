<%= simple_form_for [restaurant, review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: ratings %>
  <div class="text-center">
    <%= f.submit class: 'btn btn-dark' %>
  </div>
<% end %>