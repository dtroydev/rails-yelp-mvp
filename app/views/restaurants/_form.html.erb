<%= simple_form_for(restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: categories %>
  <div class="text-center">
    <%= f.submit "Add Restaurant", class: 'btn btn-dark' %>
  </div>
<% end %>